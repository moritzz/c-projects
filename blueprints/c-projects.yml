title: Projects

pages:
  template: c-project
  num: zero

fields:
  title:
    label: Title
    type:  text

  intro:
    label: Intro
    type:  textarea
    
  adapter:
    label: Project origin
    type: select
    required: true
    options:
      structure: "List below"
      children: "Child pages"
    default: structure
      
  projects:
    label: Projects
    type: structure
    entry: >
      {{date}} - {{date_to}} : <strong>{{title}}</strong><br />
      {{summary}}
    fields:
      title:
        label: Title
        type: text
        required: true
      category:
        extends: category
      where:
        label: Where
        type: text
      date_span: 
        extends: date_span
      summary:
        label: Summary
        type: textarea
        required: true
  
  text:
    label: Text
    type:  textarea